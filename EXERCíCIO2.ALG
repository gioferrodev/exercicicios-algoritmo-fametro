algoritmo "exercicio2"
var
  i, somaTotal:inteiro
inicio
  i <- 1
  somaTotal <- 0
  enquanto i <= 100 faca
  somaTotal <- somaTotal + i
  i <- i + 1
  fimEnquanto
  escreval("A soma dos 100 primeiros números é ", somaTotal)
fimalgoritmo